# Archivo de ejemplo de variables de entorno
# Copiar a .env en la raíz del proyecto y completar con valores reales
# IMPORTANTE: Nunca subir .env al repositorio

# Django
SECRET_KEY=tu-secret-key-super-segura-aqui-cambiar-en-produccion
DEBUG=False
ALLOWED_HOSTS=tudominio.com,www.tudominio.com,127.0.0.1

# Base de datos PostgreSQL
DB_NAME=proyecto_db
DB_USER=proyecto_user
DB_PASSWORD=password-segura-aqui
DB_HOST=localhost
DB_PORT=5432

# Opcional: Email (si usas envío de emails)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=tu-email@gmail.com
EMAIL_HOST_PASSWORD=tu-password-de-app

# Opcional: Redis (si usas cache)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0

